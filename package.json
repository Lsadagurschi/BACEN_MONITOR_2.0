{
  "name": "bacen-monitor",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/types",
    "packages/cadoc-engine"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/web",
    "dev": "npm run dev --workspace=apps/web",
    "start": "npm run start --workspace=apps/web"
  },
  "dependencies": {
    "next": "^14.2.35",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}